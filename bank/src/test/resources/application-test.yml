spring:
  datasource:
    url: jdbc:h2:mem:accounts;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.format_sql: true
  sql:
    init:
      mode: always
      data-locations: classpath:data.sql